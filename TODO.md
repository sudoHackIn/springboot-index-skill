# TODO

- [ ] Добавить в `spring-autoconfig-index-lookup` поддержку property-gates: учитывать runtime значения пропертей (`application*.yml/properties` или `/actuator/env`) при оценке `OnProperty` (`havingValue`, `matchIfMissing`) и выдавать `property_gates` + `effective_decision` в ответе.
